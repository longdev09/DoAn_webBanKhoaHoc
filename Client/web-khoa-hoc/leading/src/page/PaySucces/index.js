import classNames from "classnames/bind";
import style from "./PaySucces.module.scss";
const cx = classNames.bind(style);

export default function PaySucces() {
  return (
    <div className={cx("wrapper")}>
      <div className={cx("ssd")}>
        <table>dsd</table>
      </div>
    </div>
  );
}
